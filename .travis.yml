sudo: required
services:
  - docker
env:
  global:
    - SHA=$(git rev-parse HEAD)
before_install:
  - docker-compose -f docker-compose.yml --remove-orphans --build
  - docker run -it --env-file .env.ci dwh_migrator mvn exec:java -Dspring.profiles.active=integration-test

script:
  - echo "hello"

deploy:
  provider: script
  script: bash ./deploy.sh
  on:
    branch: master