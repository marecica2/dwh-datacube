spring:
  profiles:
    active: development
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2.console.enabled: true
logging:
  level:
    root: info
    org.bmsource: trace
  file: dwh.log
dwh:
  property: testvalue
